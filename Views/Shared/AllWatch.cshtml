@model Watch 
   
    <div class=" col-lg-4">
    <img class=" img-thumbnail" src="@Model.img" alt="@Model.name" />
    <h2> Модель:@Model.name </h2>
    <p>Стоимость: @Model.price.ToString("c")</p>
    <p><a class=" btn btn-outline-dark" href="#"> Подробнее </a></p>
    <p><a class=" btn btn-dark" href="#"> Купить </a></p>
</div>

        }